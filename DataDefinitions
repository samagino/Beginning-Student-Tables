Data Definitions:
	A Table is 
		{examples:	[Example],
		 input_names:	[String],	(formal parameters)
		 exprs:		[Column]}
	A Column is one of
		- String			(non-boolean)
		- {question:	String,		(boolean)
		   exprs:	[Column]}
	An Example is
		{inputs:	[String],
		 exprs:		[Column]}

My Data Definitions:
        A Table is
                {examples:      [Example],
                 fexprs:        [Fexpr],
                 params:        [String],
                 name:          String}

        An Example is
                {inTexts:           [String],
                 wantText:          String}

        A Fexpr is one of
                - Null
                - {text:              String,
                   outExprs:          [outExpr],
                   thenChildren:      [Fexpr],
                   elseChild:         Fexpr}

        An outExpr is
                {error: Boolean
                 expr: Antything}
            