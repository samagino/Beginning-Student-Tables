--___TODO___--

todo:
pretty printing
add unparse function that turns list into (list a b c ...)
images (svg/canvas?)

bugs:

stuff that may be nice:
undo/redo button

stuff that will probably happen eventually:
actually figrure out where/how to host this